apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: default
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80        # ALB가 접근할 포트
      targetPort: 8080 # Pod 컨테이너 포트
  type: ClusterIP