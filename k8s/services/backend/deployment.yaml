apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: backend-image:latest
          ports:
            - containerPort: 8080
